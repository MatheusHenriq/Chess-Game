import 'package:flutter/material.dart';

class ChessPiece extends StatelessWidget {
  const ChessPiece({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
